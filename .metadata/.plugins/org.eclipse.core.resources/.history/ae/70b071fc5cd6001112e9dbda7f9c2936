package com.mntripclient;

import java.io.FileNotFoundException;
import java.io.FileOutputStream;

import android.R.string;

public final class FileLog {
	private final static FileLog instance = new FileLog();
	private static FileOutputStream fileOutpurStream;
	
    public static FileLog getInstance() {
        return instance;
    }
	
	static void log(string s) throws FileNotFoundException {
		//fileOutpurStream.wr;
	}
	
	private FileLog() {
		try {
			fileOutpurStream = new FileOutputStream("nMTRIPClient.log");
		} catch (FileNotFoundException e) {
			e.printStackTrace();
		}
	}
}
